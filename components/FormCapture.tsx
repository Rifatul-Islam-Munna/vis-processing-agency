"use client"; import {useEffect} from "react";
export default function FormCapture({slug}:{slug:string}){useEffect(()=>{const listener=async(e:SubmitEvent)=>{const form=e.target as HTMLFormElement;if(!(form instanceof HTMLFormElement)||form.dataset.noCapture!==undefined||(form.method||"get").toLowerCase()==="get")return;e.preventDefault();const data=Object.fromEntries(new FormData(form).entries());const r=await fetch("/api/enquiries",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({pageSlug:slug,...data,data})});alert(r.ok?"Thank you. Your message has been received.":"Unable to submit. Please try again.");if(r.ok)form.reset()};document.addEventListener("submit",listener);return()=>document.removeEventListener("submit",listener)},[slug]);return null}
