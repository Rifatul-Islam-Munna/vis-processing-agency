import {NextResponse} from "next/server";import {getSession} from "@/lib/auth";import {connectDB} from "@/lib/db";import {Enquiry} from "@/lib/models";export async function POST(req:Request){try{const b=await req.json();const u=await getSession();await connectDB();const row=await Enquiry.create({pageSlug:String(b.pageSlug||""),name:String(b.name||b.fullname||""),email:String(b.email||""),phone:String(b.phone||""),subject:String(b.subject||""),message:String(b.message||b.comment||""),data:b.data||b,userId:u?.id});return NextResponse.json({ok:true,id:String(row._id)})}catch{return NextResponse.json({error:"Submission failed"},{status:400})}}
