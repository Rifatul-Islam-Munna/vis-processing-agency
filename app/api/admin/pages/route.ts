import {NextResponse} from "next/server";import {requireAdmin} from "@/lib/auth";import {connectDB} from "@/lib/db";import {Page} from "@/lib/models";export async function POST(req:Request){await requireAdmin();try{await connectDB();const page=await Page.create(await req.json());return NextResponse.json({page:JSON.parse(JSON.stringify(page))})}catch(e:any){return NextResponse.json({error:e.message},{status:400})}}
