"use client";import {useState} from "react";export default function GlobalReplace(){const [msg,setMsg]=useState("");return <form className="space-y-4" onSubmit={async e=>{e.preventDefault();if(!confirm("Replace this text across every saved page?"))return;const body=Object.fromEntries(new FormData(e.currentTarget).entries());const r=await fetch("/api/admin/global-replace",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});const j=await r.json();setMsg(r.ok?`${j.modified} pages updated`:j.error)}}><label className="block"><span className="mb-1 block text-sm font-bold">Find exact text</span><textarea required name="find" rows={5} className="w-full rounded-xl border border-slate-300 p-4"/></label><label className="block"><span className="mb-1 block text-sm font-bold">Replace with</span><textarea name="replace" rows={5} className="w-full rounded-xl border border-slate-300 p-4"/></label><button className="rounded-xl bg-indigo-600 px-6 py-3 font-bold text-white">Replace across all pages</button><p className="font-bold text-emerald-700">{msg}</p></form>}
