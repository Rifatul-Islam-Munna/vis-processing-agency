import {notFound} from "next/navigation";import PageEditor from "@/components/PageEditor";import {connectDB,hasDatabase} from "@/lib/db";import {Page} from "@/lib/models";import {defaultPages} from "@/content/page-index";
export default async function Edit({params}:{params:Promise<{id:string}>}){const {id}=await params;let page:any=null;if(hasDatabase())try{await connectDB();page=await Page.findOne(/^[a-f0-9]{24}$/i.test(id)?{_id:id}:{slug:id}).lean()}catch{}if(!page)page=defaultPages[id];if(!page)notFound();page=JSON.parse(JSON.stringify(page));return <><h1 className="mb-6 text-3xl font-black">Edit page</h1><PageEditor page={page}/></>}
