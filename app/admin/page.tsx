import AdminCard from "@/components/AdminCard";import {connectDB,hasDatabase} from "@/lib/db";import {Page,User,Enquiry,Media} from "@/lib/models";import {defaultPageList} from "@/content/page-index";
export default async function Dashboard(){let counts={pages:defaultPageList.length,users:0,enquiries:0,media:0};let db=false;if(hasDatabase())try{await connectDB();db=true;const [pages,users,enquiries,media]=await Promise.all([Page.countDocuments(),User.countDocuments(),Enquiry.countDocuments(),Media.countDocuments()]);counts={pages:pages||defaultPageList.length,users,enquiries,media}}catch{}return <><div className="mb-8 flex flex-wrap items-end justify-between gap-3"><div><h1 className="text-3xl font-black">Dashboard</h1><p className="mt-1 text-slate-500">Manage every page, SEO field, user and submission.</p></div><span className={`rounded-full px-3 py-1 text-xs font-bold ${db?"bg-emerald-100 text-emerald-700":"bg-amber-100 text-amber-800"}`}>{db?"MongoDB connected":"Using static fallback"}</span></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><AdminCard title="Pages" value={counts.pages} href="/admin/pages"/><AdminCard title="Users" value={counts.users} href="/admin/users"/><AdminCard title="Enquiries" value={counts.enquiries} href="/admin/enquiries"/><AdminCard title="Media items" value={counts.media} href="/admin/media"/></div>{!db&&<div className="mt-6 rounded-2xl border border-amber-200 bg-amber-50 p-5 text-amber-900"><b>Database setup required:</b> copy <code>.env.example</code> to <code>.env.local</code>, set <code>MONGODB_URI</code>, then run <code>npm run seed</code>.</div>}</>}
