import {NextResponse} from "next/server";import {getSession} from "@/lib/auth";import {connectDB,hasDatabase} from "@/lib/db";import {Activity} from "@/lib/models";export async function POST(req:Request){const u=await getSession();if(!u||!hasDatabase())return NextResponse.json({ok:true});try{const b=await req.json();await connectDB();await Activity.create({userId:u.id,type:"page_view",pageSlug:String(b.slug||""),path:String(b.path||"")});return NextResponse.json({ok:true})}catch{return NextResponse.json({ok:false},{status:400})}}
