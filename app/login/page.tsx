import AuthForm from "@/components/AuthForm"; import {getSession} from "@/lib/auth"; import {redirect} from "next/navigation";
export default async function Login(){const s=await getSession();if(s)redirect(s.role==="admin"?"/admin":"/account");return <main className="admin-ui flex min-h-screen items-center justify-center p-6"><section className="w-full max-w-md rounded-2xl bg-white p-8 shadow-xl"><a href="/" className="text-sm font-semibold text-indigo-600">← Back to website</a><h1 className="mt-6 text-3xl font-black">Welcome back</h1><p className="mb-6 mt-2 text-slate-500">Sign in to access your account.</p><AuthForm mode="login"/><p className="mt-6 text-center text-sm text-slate-500">Need an account? <a className="font-bold text-indigo-600" href="/register">Register</a></p></section></main>}
