import AuthForm from "@/components/AuthForm"; import {notFound} from "next/navigation";
export default function Register(){if(process.env.ALLOW_REGISTRATION==="false")notFound();return <main className="admin-ui flex min-h-screen items-center justify-center p-6"><section className="w-full max-w-md rounded-2xl bg-white p-8 shadow-xl"><a href="/" className="text-sm font-semibold text-indigo-600">← Back to website</a><h1 className="mt-6 text-3xl font-black">Create account</h1><p className="mb-6 mt-2 text-slate-500">Track your enquiries and browsing history.</p><AuthForm mode="register"/><p className="mt-6 text-center text-sm text-slate-500">Already registered? <a className="font-bold text-indigo-600" href="/login">Sign in</a></p></section></main>}
