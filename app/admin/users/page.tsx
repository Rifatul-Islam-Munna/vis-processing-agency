import {connectDB,hasDatabase} from "@/lib/db";import {User} from "@/lib/models";export default async function Users(){let rows:any[]=[];if(hasDatabase())try{await connectDB();rows=await User.find({}).sort({createdAt:-1}).lean()}catch{}return <><h1 className="text-3xl font-black">Users</h1><p className="mb-6 mt-1 text-slate-500">Registered clients and administrators.</p><div className="overflow-hidden rounded-2xl border border-slate-200 bg-white"><table className="w-full text-left"><thead className="bg-slate-50"><tr><th className="p-4">Name</th><th className="p-4">Email</th><th className="p-4">Role</th><th className="p-4">Joined</th></tr></thead><tbody>{rows.map((u:any)=><tr key={String(u._id)} className="border-t border-slate-100"><td className="p-4 font-bold">{u.name}</td><td className="p-4">{u.email}</td><td className="p-4">{u.role}</td><td className="p-4 text-sm text-slate-500">{new Date(u.createdAt).toLocaleDateString()}</td></tr>)}</tbody></table></div></>}
