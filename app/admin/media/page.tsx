import MediaForm from "@/components/MediaForm";import {connectDB,hasDatabase} from "@/lib/db";import {Media} from "@/lib/models";export default async function MediaPage(){let rows:any[]=[];if(hasDatabase())try{await connectDB();rows=await Media.find({}).sort({createdAt:-1}).lean()}catch{}return <><h1 className="text-3xl font-black">Media library</h1><p className="mb-6 mt-1 text-slate-500">Register reusable image and document URLs, including included template assets.</p><MediaForm/><div className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{rows.map((m:any)=><article key={String(m._id)} className="overflow-hidden rounded-2xl border border-slate-200 bg-white"><div className="aspect-video bg-slate-100">{m.type==="image"&&<img src={m.url} alt={m.alt||m.name} className="h-full w-full object-cover"/>}</div><div className="p-4"><h2 className="font-bold">{m.name}</h2><code className="mt-2 block break-all text-xs text-slate-500">{m.url}</code></div></article>)}</div></>}
