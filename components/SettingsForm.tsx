"use client";import {useState} from "react";export default function SettingsForm({settings}:{settings:any}){const [msg,setMsg]=useState("");return <form className="space-y-4" onSubmit={async e=>{e.preventDefault();const value=Object.fromEntries(new FormData(e.currentTarget).entries());const r=await fetch("/api/admin/settings",{method:"PUT",headers:{"content-type":"application/json"},body:JSON.stringify(value)});setMsg(r.ok?"Settings saved":"Save failed")}}>{[["Site name","name"],["Primary email","email"],["Support email","supportEmail"],["Phone","phone"],["Address","address"],["Public site URL","siteUrl"]].map(([l,n])=><label key={n} className="block"><span className="mb-1.5 block text-sm font-bold">{l}</span><input name={n} defaultValue={settings[n]||""} className="w-full rounded-xl border border-slate-300 px-4 py-3"/></label>)}{[["Global head HTML","customHead"],["Global CSS","customCss"],["Global JavaScript","customJs"]].map(([l,n])=><label key={n} className="block"><span className="mb-1.5 block text-sm font-bold">{l}</span><textarea rows={8} name={n} defaultValue={settings[n]||""} className="w-full rounded-xl border border-slate-300 px-4 py-3 font-mono text-sm"/></label>)}<button className="rounded-xl bg-indigo-600 px-6 py-3 font-bold text-white">Save settings</button><span className="ml-3 text-sm font-bold text-emerald-700">{msg}</span></form>}
