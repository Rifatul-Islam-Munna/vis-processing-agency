"use client";import {useState} from "react";
export default function PageEditor({page,isNew=false}:{page:any;isNew?:boolean}){const [busy,setBusy]=useState(false);const [msg,setMsg]=useState("");return <form className="space-y-6" onSubmit={async e=>{e.preventDefault();setBusy(true);setMsg("");const fd=new FormData(e.currentTarget);const seo={title:fd.get("seoTitle"),description:fd.get("seoDescription"),keywords:fd.get("keywords"),canonical:fd.get("canonical"),ogTitle:fd.get("ogTitle"),ogImage:fd.get("ogImage"),noIndex:fd.get("noIndex")==="on",jsonLd:fd.get("jsonLd")};const body={title:fd.get("title"),slug:fd.get("slug"),published:fd.get("published")==="on",html:fd.get("html"),headHtml:fd.get("headHtml"),customCss:fd.get("customCss"),customJs:fd.get("customJs"),seo};const r=await fetch(isNew?"/api/admin/pages":`/api/admin/pages/${page._id||page.slug}`,{method:isNew?"POST":"PUT",headers:{"content-type":"application/json"},body:JSON.stringify(body)});const j=await r.json().catch(()=>({}));setBusy(false);if(!r.ok)return setMsg(j.error||"Save failed");setMsg("Saved successfully");if(isNew)location.href=`/admin/pages/${j.page._id}`}}><div className="grid gap-4 md:grid-cols-2"><Field label="Page title" name="title" value={page.title}/><Field label="URL slug" name="slug" value={page.slug}/></div><label className="flex items-center gap-2 text-sm font-bold"><input type="checkbox" name="published" defaultChecked={page.published!==false}/> Published</label><Area label="Complete page HTML" name="html" value={page.html} rows={24}/><div className="grid gap-4 xl:grid-cols-2"><Area label="Page-specific head HTML" name="headHtml" value={page.headHtml}/><Area label="Page-specific CSS" name="customCss" value={page.customCss}/><Area label="Page-specific JavaScript" name="customJs" value={page.customJs}/><Area label="JSON-LD structured data" name="jsonLd" value={page.seo?.jsonLd}/></div><section className="rounded-2xl border border-slate-200 bg-white p-5"><h2 className="mb-4 text-xl font-black">SEO and social sharing</h2><div className="grid gap-4 md:grid-cols-2"><Field label="SEO title" name="seoTitle" value={page.seo?.title}/><Field label="Open Graph title" name="ogTitle" value={page.seo?.ogTitle}/><Field label="Keywords" name="keywords" value={page.seo?.keywords}/><Field label="Canonical URL" name="canonical" value={page.seo?.canonical}/><Field label="Open Graph image" name="ogImage" value={page.seo?.ogImage}/><label className="flex items-center gap-2 pt-7 text-sm font-bold"><input type="checkbox" name="noIndex" defaultChecked={page.seo?.noIndex}/> Prevent search indexing</label></div><Area label="Meta description" name="seoDescription" value={page.seo?.description} rows={4}/></section><div className="sticky bottom-4 flex items-center gap-3 rounded-2xl border border-slate-200 bg-white/95 p-4 shadow-lg backdrop-blur"><button disabled={busy} className="rounded-xl bg-indigo-600 px-6 py-3 font-bold text-white disabled:opacity-50">{busy?"Saving...":"Save page"}</button>{!isNew&&<a target="_blank" href={page.slug==="home"?"/":`/${page.slug}`} className="rounded-xl border border-slate-300 px-5 py-3 font-bold">Preview ↗</a>}<span className="text-sm font-semibold text-emerald-700">{msg}</span></div></form>}
function Field({label,name,value}:{label:string;name:string;value?:string}){return <label className="block"><span className="mb-1.5 block text-sm font-bold">{label}</span><input name={name} defaultValue={value||""} className="w-full rounded-xl border border-slate-300 bg-white px-4 py-3"/></label>}
function Area({label,name,value,rows=8}:{label:string;name:string;value?:string;rows?:number}){return <label className="mt-4 block"><span className="mb-1.5 block text-sm font-bold">{label}</span><textarea name={name} rows={rows} defaultValue={value||""} className="w-full rounded-xl border border-slate-300 bg-white px-4 py-3 font-mono text-sm"/></label>}
